# 2025-10-03

## Initial Project Setup

### Directory Structure
- Created `src/` for source files
- Created `include/` for header files
- Created `conf/` for configuration files
- Created `changelog/` for daily change logs
- Created `docs/` for project documentation

### Build System
- Fixed misspelled `Makeflie` → deleted
- Created proper `Makefile` with:
  - C++98 compilation flags (`-Wall -Wextra -Werror -std=c++98`)
  - Standard rules: `all`, `clean`, `fclean`, `re`
  - Object files directory (`obj/`)
  - Include path configuration

### Server Implementation
- Created `src/main.cpp` with basic socket server:
  - Opens socket on port 8080
  - Sets non-blocking mode with `fcntl()`
  - Enables `SO_REUSEADDR` for socket reuse
  - Binds to `INADDR_ANY` (all interfaces)
  - Listens for incoming connections
  - Accepts one connection
  - Sends "Hello from Webserv!" message
  - Closes connection and exits

### Configuration
- Updated `.gitignore` to exclude:
  - `webserv` executable
  - `obj/` directory

### Testing
- Compiled successfully with `make`
- Server ready to test with `telnet localhost 8080`

### Cross-Platform Compatibility
- Added explicit cast to `setsockopt()` for compatibility between Linux and macOS
- Created `socket_utils.hpp` and `socket_utils.cpp` for cross-platform utilities
- Implemented `setNonBlocking()` function with OS detection:
  - macOS: Uses `fcntl()` with `F_SETFL` and `O_NONBLOCK`
  - Linux: Uses `setsockopt()` with `SO_NONBLOCK`
- Updated `main.cpp` to use centralized utility function
- Code uses POSIX-compliant system calls that work on both platforms
- Verified compilation with C++98 strict mode

### Bug Fixes
- Fixed non-blocking socket issue with `accept()`
- Added `poll()` to wait for incoming connections before calling `accept()`
- Server now properly waits for client connections without busy-waiting
- Timeout set to -1 (infinite) for `poll()` to block until connection arrives

### Documentation
- Created `docs/port_listening.md` explaining:
  - Current socket setup and configuration
  - Step-by-step process of listening and accepting connections
  - How `poll()` is used for event-driven I/O
  - Testing instructions with telnet
  - Common issues and solutions
  - Future enhancements for multiple ports and clients
